### TO RUN:
./setup.sh
./run.sh

Once in the environment run:
make test-x TEST=hello-world

If this works you can try different tests (ex fibonacci) or switch to testing
either just the s core (test-s) or both (test-dual)
