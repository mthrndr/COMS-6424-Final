From ee01045048ea8638c629fdc8d8a5c811a9adb1c9 Mon Sep 17 00:00:00 2001
From: Jayden <jayleesin@gmail.com>
Date: Tue, 5 May 2026 22:11:08 -0400
Subject: [PATCH 1/4] x core tb pin renaming

---
 cv32e40x/tb/core/cv32e40x_tb_wrapper.sv | 1 -
 1 file changed, 1 deletion(-)

diff --git a/cv32e40x/tb/core/cv32e40x_tb_wrapper.sv b/cv32e40x/tb/core/cv32e40x_tb_wrapper.sv
index c73af30a..43ece4ec 100644
--- a/cv32e40x/tb/core/cv32e40x_tb_wrapper.sv
+++ b/cv32e40x/tb/core/cv32e40x_tb_wrapper.sv
@@ -141,7 +141,6 @@ module cv32e40x_tb_wrapper
 
         // Event wakeup signals
         .wu_wfe_i               ( 1'b0                  ),
-        .wu_wrs_i               ( 1'b0                  ),
 
         .clic_irq_i             (  '0                   ),
         .clic_irq_id_i          (  '0                   ),
-- 
2.54.0


From a0f679899f0248a0cb454aacd3164745cd162bcd Mon Sep 17 00:00:00 2001
From: Jayden <jayleesin@gmail.com>
Date: Tue, 5 May 2026 23:04:02 -0400
Subject: [PATCH 2/4] Unsuccessful hello-world for s core. Compiles fine but
 hangs due to security feature creating a trap

---
 cv32e40s/bsp/crt0.S                     |   3 -
 cv32e40s/sim/core/Makefile              |   9 +-
 cv32e40s/tb/core/cv32e40s_tb_wrapper.sv | 122 ++++++++++++++++--------
 cv32e40s/tb/core/mm_ram.sv              |   6 +-
 4 files changed, 95 insertions(+), 45 deletions(-)

diff --git a/cv32e40s/bsp/crt0.S b/cv32e40s/bsp/crt0.S
index 91a77b55..a7748ea0 100644
--- a/cv32e40s/bsp/crt0.S
+++ b/cv32e40s/bsp/crt0.S
@@ -28,9 +28,6 @@ _start:
   addi  gp, gp, %pcrel_lo(1b)
 
 /* initialize vector table pointer */
-1:auipc a0, %pcrel_hi(__jvt_base$)
-  addi  a0, a0, %pcrel_lo(1b)
-  csrw  jvt, a0
 .option pop
 
 /* initialize stack pointer */
diff --git a/cv32e40s/sim/core/Makefile b/cv32e40s/sim/core/Makefile
index a13c2147..90d20359 100644
--- a/cv32e40s/sim/core/Makefile
+++ b/cv32e40s/sim/core/Makefile
@@ -46,6 +46,13 @@ MAKE_DIR       = $(PWD)
 MAKE_PATH     := $(shell dirname $(realpath $(firstword $(MAKEFILE_LIST))))
 COREV_PROJ     = cv32
 CORE_V_VERIF   = $(abspath $(MAKE_PATH)/../../..)
+RUN_INDEX                ?= 0
+SIM_RESULTS              = simulation_results
+SIM_TEST_RESULTS         = $(SIM_RESULTS)/$(TEST)
+SIM_RUN_RESULTS          = $(SIM_TEST_RESULTS)/$(RUN_INDEX)
+SIM_TEST_PROGRAM_RESULTS = $(SIM_RUN_RESULTS)/test_program
+SIM_BSP_RESULTS          = $(SIM_TEST_PROGRAM_RESULTS)/bsp
+VERI_CUSTOM              = $(SIM_RESULTS)
 DATE           = $(shell date +%F)
 WAVES          = 0
 
@@ -127,7 +134,7 @@ VERI_FLAGS         +=
 VERI_COMPILE_FLAGS += -Wno-BLKANDNBLK +define+COREV_ASSERT_OFF $(SV_CMP_FLAGS) # hope this doesn't hurt us in the long run
 VERI_TRACE         ?=
 VERI_OBJ_DIR       ?= cobj_dir
-VERI_LOG_DIR       ?= cobj_dir/logs
+VERI_LOG_DIR       ?= $(SIM_TEST_PROGRAM_RESULTS)
 VERI_CFLAGS        += -O2
 
 #riviera configuration
diff --git a/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv b/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
index 3a116df7..1ff68d1b 100644
--- a/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
+++ b/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
@@ -80,44 +80,90 @@ module cv32e40s_tb_wrapper
 //      );
 
     // instantiate the core
-    cv32e40s_core cv32e40s_core_i
-        (
-         .clk_i                  ( clk_i                 ),
-         .rst_ni                 ( rst_ni                ),
-         
-         .scan_cg_en_i           ( '0                    ),
-
-         .boot_addr_i            ( BOOT_ADDR             ),
-         .dm_halt_addr_i         ( DM_HALTADDRESS        ),
-         .mhartid_i              ( HART_ID               ),
-         .mimpid_patch_i         ( IMP_PATCH_ID          ),
-
-         .instr_req_o            ( instr_req             ),
-         .instr_gnt_i            ( instr_gnt             ),
-         .instr_rvalid_i         ( instr_rvalid          ),
-         .instr_addr_o           ( instr_addr            ),
-         .instr_rdata_i          ( instr_rdata           ),
-
-         .data_req_o             ( data_req              ),
-         .data_gnt_i             ( data_gnt              ),
-         .data_rvalid_i          ( data_rvalid           ),
-         .data_we_o              ( data_we               ),
-         .data_be_o              ( data_be               ),
-         .data_addr_o            ( data_addr             ),
-         .data_wdata_o           ( data_wdata            ),
-         .data_rdata_i           ( data_rdata            ),
-
-         // Interrupts verified in UVM environment
-         .irq_i                  ( {32{1'b0}}            ),
-         .irq_ack_o              ( irq_ack               ),
-         .irq_id_o               ( irq_id_out            ),
-
-         .debug_req_i            ( debug_req             ),
-
-         .fetch_enable_i         ( fetch_enable_i        ),
-         .core_sleep_o           ( core_sleep_o          )
-       );
-
+    cv32e40s_core
+   	#(.SMCLIC		(0),
+	  .PMA_NUM_REGIONS	(0),
+	  .PMP_NUM_REGIONS	(0)
+  	 )
+    cv32e40s_core_i
+    	 (.clk_i                  ( clk_i                 ),
+	  .rst_ni                 ( rst_ni                ),
+	  .scan_cg_en_i           ( '0                    ),
+
+	  .boot_addr_i            ( BOOT_ADDR             ),
+	  .dm_exception_addr_i    ( '0                    ),
+	  .dm_halt_addr_i         ( DM_HALTADDRESS        ),
+	  .mhartid_i              ( HART_ID               ),
+	  .mimpid_patch_i         ( IMP_PATCH_ID          ),
+	  .mtvec_addr_i           ( '0                    ),
+
+	  .instr_req_o            ( instr_req             ),
+	  .instr_gnt_i            ( instr_gnt             ),
+	  .instr_rvalid_i         ( instr_rvalid          ),
+	  .instr_addr_o           ( instr_addr            ),
+	  .instr_memtype_o        (                       ),
+	  .instr_prot_o           (                       ),
+	  .instr_dbg_o            (                       ),
+	  .instr_rdata_i          ( instr_rdata           ),
+	  .instr_err_i            ( 1'b0                  ),
+	  .instr_reqpar_o         (                       ),
+	  .instr_gntpar_i         ( 1'b0                  ),
+	  .instr_rvalidpar_i      ( 1'b0                  ),
+	  .instr_achk_o           (                       ),
+	  .instr_rchk_i           ( '0                    ),
+
+	  .data_req_o             ( data_req              ),
+	  .data_gnt_i             ( data_gnt              ),
+	  .data_rvalid_i          ( data_rvalid           ),
+	  .data_addr_o            ( data_addr             ),
+	  .data_be_o              ( data_be               ),
+	  .data_we_o              ( data_we               ),
+	  .data_wdata_o           ( data_wdata            ),
+	  .data_memtype_o         (                       ),
+	  .data_prot_o            (                       ),
+	  .data_dbg_o             (                       ),
+	  .data_rdata_i           ( data_rdata            ),
+	  .data_err_i             ( 1'b0                  ),
+	  .data_reqpar_o          (                       ),
+	  .data_gntpar_i          ( 1'b0                  ),
+	  .data_rvalidpar_i       ( 1'b0                  ),
+	  .data_achk_o            (                       ),
+	  .data_rchk_i            ( '0                    ),
+
+	  .mcycle_o               (                       ),
+
+	  .irq_i                  ( {32{1'b0}}            ),
+	  .wu_wfe_i               ( 1'b0                  ),
+	  .clic_irq_i             ( 1'b0                  ),
+	  .clic_irq_id_i          ( '0                    ),
+	  .clic_irq_level_i       ( '0                    ),
+	  .clic_irq_priv_i        ( '0                    ),
+	  .clic_irq_shv_i         ( 1'b0                  ),
+
+	  .fencei_flush_req_o     (                       ),
+	  .fencei_flush_ack_i     ( 1'b1                  ),
+
+	  .alert_minor_o          (                       ),
+	  .alert_major_o          (                       ),
+
+	  .debug_req_i            ( debug_req             ),
+	  .debug_havereset_o      (                       ),
+	  .debug_running_o        (                       ),
+	  .debug_halted_o         (                       ),
+
+	  .fetch_enable_i         ( fetch_enable_i        ),
+	  .core_sleep_o           ( core_sleep_o          )
+    	 );
+    //Debugging
+    int unsigned dbg_cycles = 0;
+    always_ff @(posedge clk_i) begin
+	if(rst_ni) begin
+		dbg_cycles <= dbg_cycles + 1;
+		if(dbg_cycles < 100 || (dbg_cycles % 1000 == 0)) begin
+			$display("[%0t] cyc=%0d pc=%h irq=%b dreq=%b we=%b daddr=%h slp=%b mcause=%h", $time, dbg_cycles, instr_addr, instr_req, data_req, data_we, data_addr, core_sleep_o, cv32e40s_core_i.cs_registers_i.mcause_q);
+		end
+	end
+    end
     // this handles read to RAM and memory mapped pseudo peripherals
     mm_ram
         #(.RAM_ADDR_WIDTH (RAM_ADDR_WIDTH),
diff --git a/cv32e40s/tb/core/mm_ram.sv b/cv32e40s/tb/core/mm_ram.sv
index d2457d47..75a442c6 100644
--- a/cv32e40s/tb/core/mm_ram.sv
+++ b/cv32e40s/tb/core/mm_ram.sv
@@ -75,9 +75,9 @@ module mm_ram
 
     localparam int                        RND_IRQ_ID     = 31;
 
-    localparam int                        MMADDR_PRINT          = 32'h1000_0000;
-    localparam int                        MMADDR_TESTSTATUS     = 32'h2000_0000;
-    localparam int                        MMADDR_EXIT           = 32'h2000_0004;
+    localparam int                        MMADDR_PRINT      = 32'h0080_0000;
+    localparam int                        MMADDR_TESTSTATUS = 32'h0080_00c0;
+    localparam int                        MMADDR_EXIT       = 32'h0080_00c4;
     localparam int                        MMADDR_SIGBEGIN       = 32'h2000_0008;
     localparam int                        MMADDR_SIGEND         = 32'h2000_000C;
     localparam int                        MMADDR_SIGDUMP        = 32'h2000_0010;
-- 
2.54.0


From 644fb0b09c6040223324ee30f418140124a8f354 Mon Sep 17 00:00:00 2001
From: mthrndr <kristian.d.nikolov@gmail.com>
Date: Tue, 12 May 2026 00:55:01 -0400
Subject: [PATCH 3/4] Fixed s sim

---
 cv32e40s/sim/core/Makefile              |   4 +-
 cv32e40s/tb/core/cv32e40s_tb_wrapper.sv | 155 +++++++++++++++++++++---
 cv32e40s/tb/core/tb_top_verilator.cpp   |   9 +-
 cv32e40x/tb/core/cv32e40x_tb_wrapper.sv |  12 +-
 4 files changed, 156 insertions(+), 24 deletions(-)

diff --git a/cv32e40s/sim/core/Makefile b/cv32e40s/sim/core/Makefile
index 90d20359..5333693f 100644
--- a/cv32e40s/sim/core/Makefile
+++ b/cv32e40s/sim/core/Makefile
@@ -467,9 +467,9 @@ testbench_verilator: CV_CORE_pkg $(TBSRC_VERI) $(TBSRC_PKG)
 	$(VERILATOR) --cc --sv --exe \
 		$(VERI_TRACE) \
 		--Wno-lint --Wno-UNOPTFLAT \
-		--Wno-MODDUP --top-module \
-		tb_top_verilator $(TBSRC_VERI) \
+		--Wno-MODDUP --top-module tb_top_verilator \
 		-f $(CV_CORE_MANIFEST) \
+		$(TBSRC_VERI) \
 		$(CV_CORE_PKG)/bhv/$(CV_CORE_LC)_core_log.sv \
 		$(TBSRC_CORE)/tb_top_verilator.cpp --Mdir $(VERI_OBJ_DIR) \
 		-CFLAGS "-std=gnu++11 $(VERI_CFLAGS)" \
diff --git a/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv b/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
index 1ff68d1b..f4b3fcbe 100644
--- a/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
+++ b/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
@@ -79,14 +79,55 @@ module cv32e40s_tb_wrapper
 //          .pc_id_i            ( cv32e40s_core_i.pc_id                       )
 //      );
 
-    // instantiate the core
-    cv32e40s_core
-   	#(.SMCLIC		(0),
-	  .PMA_NUM_REGIONS	(0),
-	  .PMP_NUM_REGIONS	(0)
+    // Used for data and instr rchk
+    function automatic logic [4:0] calc_obi_chk(logic [31:0] data);
+        logic [4:0] chk;
+        chk[0] = ^data[7:0];    // Parity Byte 0
+        chk[1] = ^data[15:8];   // Parity Byte 1
+        chk[2] = ^data[23:16];  // Parity Byte 2
+        chk[3] = ^data[31:24];  // Parity Byte 3
+        chk[4] = 1'b0;          // Set to 0
+        return chk;
+    endfunction
+
+    import cv32e40s_pkg::*;
+    localparam pma_cfg_t TB_PMA_CFG [1] = '{
+        '{
+            word_addr_low:  32'h0000_0000,
+            word_addr_high: 32'hFFFF_FFFF,
+            main:           1'b1,          // 1 = Executable RAM
+            bufferable:     1'b1,
+            cacheable:      1'b1,
+            integrity:      1'b1           // 1 = Turn on OBI checkers
+        }
+    };
+
+    localparam pmpncfg_t TB_PMP_CFG [1] = '{
+        '{
+            lock:  1'b0,
+            zero0: 2'b00,
+            mode:  PMP_MODE_TOR,    // TOR MODE
+            exec:  1'b1,            // Grants Execute permission
+            write: 1'b1,            // Grants Write permission
+            read:  1'b1             // Grants Read permission
+        }
+    };
+
+    localparam logic [31:0] TB_PMP_ADDR [1] = '{
+        32'hFFFF_FFFF // NAPOT with all 1s covers the entire 32-bit address space
+    };
+
+    // Instantiate the core
+    cv32e40s_core #(
+        .SMCLIC		(0),
+	    .PMA_NUM_REGIONS	(1),
+        .PMA_CFG            (TB_PMA_CFG),
+	    .PMP_NUM_REGIONS	(1),
+        .PMP_PMPNCFG_RV(TB_PMP_CFG),
+        .PMP_PMPADDR_RV(TB_PMP_ADDR)
   	 )
-    cv32e40s_core_i
-    	 (.clk_i                  ( clk_i                 ),
+    cv32e40s_core_i(
+      .clk_i                  ( clk_i                 ),
 	  .rst_ni                 ( rst_ni                ),
 	  .scan_cg_en_i           ( '0                    ),
 
@@ -107,10 +148,10 @@ module cv32e40s_tb_wrapper
 	  .instr_rdata_i          ( instr_rdata           ),
 	  .instr_err_i            ( 1'b0                  ),
 	  .instr_reqpar_o         (                       ),
-	  .instr_gntpar_i         ( 1'b0                  ),
-	  .instr_rvalidpar_i      ( 1'b0                  ),
+	  .instr_gntpar_i         (~instr_gnt             ),
+	  .instr_rvalidpar_i      (~instr_rvalid          ),
 	  .instr_achk_o           (                       ),
-	  .instr_rchk_i           ( '0                    ),
+	  .instr_rchk_i           (calc_obi_chk(instr_rdata)),
 
 	  .data_req_o             ( data_req              ),
 	  .data_gnt_i             ( data_gnt              ),
@@ -125,10 +166,10 @@ module cv32e40s_tb_wrapper
 	  .data_rdata_i           ( data_rdata            ),
 	  .data_err_i             ( 1'b0                  ),
 	  .data_reqpar_o          (                       ),
-	  .data_gntpar_i          ( 1'b0                  ),
-	  .data_rvalidpar_i       ( 1'b0                  ),
+	  .data_gntpar_i          (~data_gnt              ),
+	  .data_rvalidpar_i       (~data_rvalid           ),
 	  .data_achk_o            (                       ),
-	  .data_rchk_i            ( '0                    ),
+	  .data_rchk_i            (calc_obi_chk(data_rdata)),
 
 	  .mcycle_o               (                       ),
 
@@ -155,14 +196,90 @@ module cv32e40s_tb_wrapper
 	  .core_sleep_o           ( core_sleep_o          )
     	 );
     //Debugging
+    // Checking if verilator is accepting new PMA config
+    initial begin
+        $display("--- CORE PARAMETER CHECK ---");
+        $display("PMA_NUM_REGIONS = %0d", cv32e40s_core_i.PMA_NUM_REGIONS);
+        if (cv32e40s_core_i.PMA_NUM_REGIONS > 0) begin
+            $display("PMA_CFG[0].main = %b", cv32e40s_core_i.PMA_CFG[0].main);
+            $display("PMA_CFG[0].word_addr_high = %h", cv32e40s_core_i.PMA_CFG[0].word_addr_high);
+        end
+        $display("PMP_NUM_REGIONS = %0d", cv32e40s_core_i.PMP_NUM_REGIONS);
+        if (cv32e40s_core_i.PMP_NUM_REGIONS > 0) begin
+            $display("PMP_CFG[0].exec = %b", cv32e40s_core_i.PMP_PMPNCFG_RV[0].exec);
+            $display("PMP_ADDR[0] = %h", cv32e40s_core_i.PMP_PMPADDR_RV[0]);
+        end
+        $display("----------------------------");
+    end
     int unsigned dbg_cycles = 0;
     always_ff @(posedge clk_i) begin
-	if(rst_ni) begin
-		dbg_cycles <= dbg_cycles + 1;
-		if(dbg_cycles < 100 || (dbg_cycles % 1000 == 0)) begin
-			$display("[%0t] cyc=%0d pc=%h irq=%b dreq=%b we=%b daddr=%h slp=%b mcause=%h", $time, dbg_cycles, instr_addr, instr_req, data_req, data_we, data_addr, core_sleep_o, cv32e40s_core_i.cs_registers_i.mcause_q);
-		end
-	end
+        if(rst_ni) begin
+            dbg_cycles <= dbg_cycles + 1;
+            if(dbg_cycles < 100 || (dbg_cycles % 1000 == 0)) begin
+                $display("[%0t] cyc=%0d pc=%h instr_req=%b  instr_gnt=%b data_req=%b we=%b daddr=%h slp=%b mcause=%h mepc=%h",
+                        $time,
+                        dbg_cycles,
+                        instr_addr,
+                        instr_req,
+                        instr_gnt,
+                        data_req,
+                        data_we,
+                        data_addr,
+                        core_sleep_o,
+                        cv32e40s_core_i.cs_registers_i.mcause_q,
+                        cv32e40s_core_i.mepc
+                    );
+            end
+            // Check Instruction Integrity Errors
+            // Check Instruction Integrity Errors
+            if (rst_ni && cv32e40s_core_i.instr_rvalid_i) begin
+              // Path goes through if_stage_i -> instr_obi_i
+              if (cv32e40s_core_i.if_stage_i.instruction_obi_i.rchk_err_resp) begin
+                $display("[%0t] !!! INSTR INTEGRITY FAULT at PC: %h !!!", $time, cv32e40s_core_i.instr_addr_o);
+                $display("Data Seen: %h | RCHK Sent: %h | RCHK Expected: %h", 
+                         cv32e40s_core_i.instr_rdata_i, 
+                         cv32e40s_core_i.instr_rchk_i,
+                         cv32e40s_core_i.if_stage_i.instruction_obi_i.integrity_fifo_i.rchk_i.rchk_res);
+              end
+            end
+
+            // Check Data Integrity Errors (LSU)
+            if (rst_ni && cv32e40s_core_i.data_rvalid_i) begin
+              // Path goes through load_store_unit_i -> data_obi_i
+              if (cv32e40s_core_i.load_store_unit_i.data_obi_i.rchk_err_resp) begin
+                $display("[%0t] !!! DATA INTEGRITY FAULT at Addr: %h !!!", $time, cv32e40s_core_i.data_addr_o);
+                $display("Data Seen: %h | RCHK Sent: %h | RCHK Expected: %h", 
+                         cv32e40s_core_i.data_rdata_i, 
+                         cv32e40s_core_i.data_rchk_i,
+                         cv32e40s_core_i.load_store_unit_i.data_obi_i.integrity_fifo_i.rchk_i.rchk_res);
+              end
+            end
+            if (rst_ni && cv32e40s_core_i.if_stage_i.pc_if_o == 32'h00000080) begin
+                // Check if the OBI bus checker is failing
+                if (cv32e40s_core_i.if_stage_i.integrity_err_obi) begin
+                    $display("[%0t] ERROR: OBI Integrity Check Failed at 0x80!", $time);
+                end
+                // Check if the PMA/PMP (Memory Map & Security Rules) are rejecting the address
+                if (cv32e40s_core_i.if_stage_i.prefetch_inst_resp.mpu_status != 0) begin
+                    $display("[%0t] ERROR: MPU (PMA/PMP) rejected execution at 0x80! mpu_status: %0d",
+                             $time, cv32e40s_core_i.if_stage_i.prefetch_inst_resp.mpu_status);
+                end
+                if (cv32e40s_core_i.if_stage_i.mpu_i.mpu_err) begin
+                    $display("[%0t] FATAL MPU ERROR AT 0x80: pma_err=%b, pmp_err=%b",
+                    $time,
+                    cv32e40s_core_i.if_stage_i.mpu_i.pma_err,
+                    cv32e40s_core_i.if_stage_i.mpu_i.pmp_err);
+                    $display("--- INTERNAL MPU STATE ---");
+                    $display("pmp_err     = %b", cv32e40s_core_i.if_stage_i.mpu_i.pmp_err);
+                    $display("priv_lvl    = %b", cv32e40s_core_i.if_stage_i.mpu_i.priv_lvl_i);
+                    $display("cfg[0].mode = %b", cv32e40s_core_i.if_stage_i.mpu_i.csr_pmp_i.cfg[0].mode);
+                    $display("addr[0]     = %h", cv32e40s_core_i.if_stage_i.mpu_i.csr_pmp_i.addr[0]);
+                    $display("MSECCFG.MML = %b", cv32e40s_core_i.if_stage_i.mpu_i.csr_pmp_i.mseccfg.mml);
+                    $display("MSECCFG.MMWP= %b", cv32e40s_core_i.if_stage_i.mpu_i.csr_pmp_i.mseccfg.mmwp);
+                    $display("--------------------------");
+                end
+            end
+        end
     end
     // this handles read to RAM and memory mapped pseudo peripherals
     mm_ram
diff --git a/cv32e40s/tb/core/tb_top_verilator.cpp b/cv32e40s/tb/core/tb_top_verilator.cpp
index b63c47a8..a796393d 100644
--- a/cv32e40s/tb/core/tb_top_verilator.cpp
+++ b/cv32e40s/tb/core/tb_top_verilator.cpp
@@ -49,7 +49,8 @@ int main(int argc, char **argv, char **env)
 #endif
     top->fetch_enable_i = 1;
     top->clk_i          = 0;
-    top->rst_ni         = 0;
+    // Needs to start at 1 then go to 0 then back to 1
+    top->rst_ni         = 1;
 
     top->eval();
     dump_memory();
@@ -64,8 +65,12 @@ int main(int argc, char **argv, char **env)
 #endif
 
     while (!Verilated::gotFinish()) {
-        if (t > 40)
+        // Modified to go down to 0 then back up to 1
+        if (t > 5 && t <= 40) {
+            top->rst_ni = 0;
+        } else if (t > 40) {
             top->rst_ni = 1;
+        }
         top->clk_i = !top->clk_i;
         top->eval();
 #ifdef VCD_TRACE
diff --git a/cv32e40x/tb/core/cv32e40x_tb_wrapper.sv b/cv32e40x/tb/core/cv32e40x_tb_wrapper.sv
index 43ece4ec..f214f98f 100644
--- a/cv32e40x/tb/core/cv32e40x_tb_wrapper.sv
+++ b/cv32e40x/tb/core/cv32e40x_tb_wrapper.sv
@@ -162,9 +162,19 @@ module cv32e40x_tb_wrapper
 
         // CPU Control Signals
         .fetch_enable_i         ( fetch_enable_i        ),
-        .core_sleep_o           (                       )
+        .core_sleep_o           ( core_sleep_o          )
       );
 
+    //Debugging
+    int unsigned dbg_cycles = 0;
+    always_ff @(posedge clk_i) begin
+	if(rst_ni) begin
+		dbg_cycles <= dbg_cycles + 1;
+		if(dbg_cycles < 100 || (dbg_cycles % 1000 == 0)) begin
+			$display("[%0t] cyc=%0d pc=%h instr_req=%b  instr_gnt=%b data_req=%b we=%b daddr=%h slp=%b mcause=%h mepc=%h", $time, dbg_cycles, instr_addr, instr_req, instr_gnt, data_req, data_we, data_addr, core_sleep_o, cv32e40x_core_i.cs_registers_i.mcause_q, cv32e40x_core_i.mepc);
+		end
+	end
+    end
     // this handles read to RAM and memory mapped pseudo peripherals
     mm_ram
         #(.RAM_ADDR_WIDTH (RAM_ADDR_WIDTH),
-- 
2.54.0


From d8f0a8f6f54e98eba00b17a12e7609010b52c5c3 Mon Sep 17 00:00:00 2001
From: mthrndr <kristian.d.nikolov@gmail.com>
Date: Tue, 12 May 2026 01:15:31 -0400
Subject: [PATCH 4/4] Updated s core tb wrapper to work with 0.9 v of core

---
 cv32e40s/tb/core/cv32e40s_tb_wrapper.sv | 1 -
 1 file changed, 1 deletion(-)

diff --git a/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv b/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
index f4b3fcbe..e6ab5fcf 100644
--- a/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
+++ b/cv32e40s/tb/core/cv32e40s_tb_wrapper.sv
@@ -119,7 +119,6 @@ module cv32e40s_tb_wrapper
 
     // Instantiate the core
     cv32e40s_core #(
-        .SMCLIC		(0),
 	    .PMA_NUM_REGIONS	(1),
         .PMA_CFG            (TB_PMA_CFG),
 	    .PMP_NUM_REGIONS	(1),
-- 
2.54.0

